//실수를 입력 받아 소수점 둘째 자리까지 표현하는 프로그램입니다.
#include <stdio.h>

int main(void){
  float n;

  scanf("%f", &n);
  printf("%.2f\n", n);  //%.nf 형식으로 지정하면 소수점 이하 n+1째 자리에서 소수점 이하 n째 자리까지 출력하라는 의미이다.
  return 0;
}
